{
  "name": "whatsapp-integrate",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop app wrapping the whatsapp-rust library",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
